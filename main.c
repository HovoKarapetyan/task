#include <stdio.h>
#include <stdlib.h>
#include "dyn_string.h"

int main(){

    size_t size_of_dest = 6;
    size_t size_of_src = 12;

    char* dest = (cahr*)malloc(sizeof(char) * size_of_dest);
    char* dest1 = (cahr*)malloc(sizeof(char) * size_of_src);
    char* src = (cahr*)malloc(sizeof(char) * size_of_src);
 
    printf("Enter your dest");
    fget(12, );

}
